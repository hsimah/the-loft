# hosts/viking/host.conf

# ── Identity ───────────────────────────────────────────────────────
HOST_NAME="viking"

# ── Services ───────────────────────────────────────────────────────
# Production role; replacement hosts must repeat the DMZ/public-ingress checks.
SERVICES=(mushr pawst snoot houstn)

# ── Storage mount (leave empty for no mount) ───────────────────────
STORAGE_DEVICE=""
STORAGE_MOUNT=""
STORAGE_FS=""

# ── Directories (755 config dirs, 775 media dirs) ──────────────────
CONFIG_DIRS=(/opt/pawst/prod/hblake /opt/pawst/prod/hsimah)
MEDIA_DIRS=()

# ── User config ────────────────────────────────────────────────────
LITTLEDOG_EXTRA_GROUPS=""

# ── SSH ────────────────────────────────────────────────────────────
SSH_DISABLE_PASSWORD="true"

# ── Health check endpoints ────────────────────────────────────────
declare -A SERVICE_ENDPOINTS=()
declare -A SERVICE_ENDPOINTS_WARN=()
declare -A HEALTH_URLS=()
declare -A HEALTH_URLS_WARN=()

# Production artifacts are explicitly selected; never poll latest.
DEPLOY_TARGETS=()
# Setup must not start production services until the operator completes DMZ checks.
PRODUCTION_ROLE="true"

# Current Wi-Fi host uses NetworkManager; no dhcpcd fallback.
WIFI_DHCP_UNIT="NetworkManager"
